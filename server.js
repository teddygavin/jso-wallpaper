const app = require('./api/index');
const port = process.env.PORT || 3000;

app.listen(port, () => {
    console.log(`Server running on;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;; 7               ; http://localhost:${port}`);
});   55